class TreeNode:
    def __init__(self,val):
        
def getTreeInorderPreorder(inorder,preorder,s,e):
    global index
    if index < len(preorder):
        root = TreeNode(preorder[self.index])
        print preorder[self.index],s,e,
        x = inorder.index(preorder[self.index],s,e)
        print x
        self.index += 1
        root.left = self.getTreeInorderPreorder(inorder,preorder,s,x)
        root.right = self.getTreeInorderPreorder(inorder,preorder,x+1,e)
        return root    